# DuolinEx Website

当前主应用位于本目录。完整项目说明、部署方式、数据库迁移和安全注意事项请阅读仓库根目录的 [README.md](../README.md)。

快速启动：

```powershell
npm.cmd run setup
npm.cmd run dev
```

浏览器打开 `http://127.0.0.1:5173/`。本地配置写入 `.env`，数据库默认保存于 `backend/data/duolinex.db`；这两个位置都不会提交到 Git。
